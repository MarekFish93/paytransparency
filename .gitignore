node_modules/
dist/
.turbo/
*.tsbuildinfo
coverage/

# GSD runtime state (run-scoped, not project output)
.gsd/
.tsbuild/

# GSD executor worktrees — live git worktrees, never project content
.claude/worktrees/
