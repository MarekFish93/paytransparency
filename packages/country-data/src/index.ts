/**
 * `@jafn/country-data` — public entry point.
 *
 * This package ships JSON plus generated `.d.ts`. It has no runtime
 * dependencies: Zod is a devDependency used to author and validate the
 * schema at build time, and a consumer pays no runtime cost for it.
 */

export const PACKAGE_NAME = '@jafn/country-data';

/** CELEX identifier of Directive (EU) 2023/970. */
export const CELEX = '32023L0970';
